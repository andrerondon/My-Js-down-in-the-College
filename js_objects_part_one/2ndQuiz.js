// Write a function that prints out all elements of a given array

// logAll(["a", 1, "hello"]) // will print:
// a
// 1
// hello


function printElements (array){
     
      for (let i=0; i < array.length; i++ ){
        console.log(array[i]);

      }
    }
printElements(['a',1, 'hello'])
