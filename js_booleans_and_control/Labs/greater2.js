
const a = process.argv[2]

const b = process.argv[3]


if (a>b) {
console.log (`${a} "is greater than" ${b}`)

} 
else (b>a) {

    console.log (`${b} greater than ${a}`)
    }

} 

else  {

    console.log (`${b} is equal to ${a}`)
    }

// Basic JavaScript: Comparison with the Greater Than Or Equal To Operator. The greater than or equal to operator ( >= ) compares the values of two numbers. If the number to the left is greater than or equal to the number to the right, it returns true . Otherwise, it returns false .